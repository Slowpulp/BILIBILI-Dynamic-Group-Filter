{
  "name": "bilibili-timeline-focus",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "B站动态分组筛选与本地阅读辅助油猴脚本",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/core.js && node --check src/dom.js && node --check src/layout.js && node --check src/style.js && node --check src/main.js && node --check scripts/build.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test && npm run build && node --check dist/BilibiliTimeline.user.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0"
  }
}
