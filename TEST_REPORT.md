# 可用性验证报告

验证日期：2026-09-06（Asia/Shanghai）

## 结论

`BilibiliTimeline.user.js` 已通过源码语法检查、100 项核心/规则/DOM/运行时/样式自动化测试和最终单文件构建检查。此前在 B 站未登录动态页完成的只读核对结果仍记录于下文；本次 v3.0.0 没有重新进行线上核对，也没有据此扩大线上兼容性结论。

本次环境没有已登录的 B 站会话，也没有可供自动安装脚本的 Tampermonkey/Violentmonkey 扩展，因此“真实账号分组成员结果”和“两个扩展内的最终安装交互”仍需用户在自己的登录浏览器中做最后一轮手工验收。脚本不会要求或导入账号 Cookie。

## 已执行的自动化验证

运行命令：

```text
npm run verify
```

覆盖内容：

- 用户脚本元数据：HTTPS `@match`、页面上下文注入、无第三方依赖/跨域授权、版本一致性。
- 分组三态真值表：无条件、多包含并集、仅排除、包含和排除重叠、未知作者放行。
- 推广识别：明确商品组件、购物平台 App 搜索与口令、优惠价格组合、购买引导、“桃宝/某宝”和 `Copy这条信息` 等规避写法；普通价格比较、平台体验讨论、反诈提醒和商品测评保持放行，并覆盖中英文标点与换行边界。
- 抽奖识别：明确抽取行为、“抽奖：”标题、关注/转发等显式或隐式参与条件、开奖与奖品组合；单独“抽奖”、抽奖机制讨论、反诈提醒和结果新闻保持放行。
- 内容类型：转发、直播、番剧影视、视频、图文、其他和未知类型优先级。
- 本地数据：隐藏记录 TTL/数量上限/重复 ID，稍后看追踪参数去重、BV→Opus 水合迁移、置顶/数量上限，损坏设置归一化。
- DOM 适配：头部 `data-mid`、作者空间链接、Vue 2 最终回退、外层 `data-did`、时间戳 Opus 链接、转发外层作者。
- 卡片操作布局：工具栏位于外层 UP 主昵称右侧、普通流式排版不覆盖正文、长昵称省略、触控可见，昵称链接外侧安全插入、嵌套更多菜单保留、延迟标题行水合后单实例迁移，以及 SPA 离开时辅助标记清理。
- 误识别回归：正文 @ 提及/转发原作者不冒充外层作者，正文动态链接不冒充当前动态 ID，普通链接预览不冒充商品组件，脚本自有工具栏和折叠控件不参与内容识别或指纹计算。
- 完整运行时：未登录友好提示、无阻塞式 `alert`、面板打开、类型/推广/抽奖开关、疑似内容折叠与展开、折叠控件被页面重绘后自愈、同类别虚拟节点复用时重置展开状态、总开关恢复、新动态增量增强、SPA 离开时清理、快速离开/返回竞态和单实例重挂载。
- 只看隐藏：统计卡按钮、`aria-pressed`、正常/隐藏视图反转、统计值稳定、真实 DOM 隐藏原因及 `aria-describedby` 关联、隐藏模式禁用重复手动隐藏、卡片移除后即时重算、零结果退出、关闭筛选及路由/账号/设置切换后的状态清理。
- 浮动入口：左下、右下、左上、右上四个固定位置、顶部导航安全距离、选择持久化、完整重载恢复、窗口变化/可视视口偏移后的同角重定位，以及拖动和方向键不会改变位置。
- 设置迁移：旧版自由坐标按最近象限迁移到固定角落，旧左右停靠迁移到底部对应角落，非法新值回退到右下角。
- 面板定位：自动/上/下/左/右方向选择、指定方向优先、空间不足自动避让、最终视口夹取，以及字号变化后的重新定位。
- 模块折叠：关注分组、隐藏内容类型、推广与抽奖、界面与布局四个模块独立/同时收起、仅保留标题、筛选控件状态不变、旧关键词模块折叠状态迁移、持久化与重载恢复、键盘焦点接续、语义属性同步，以及折叠期间面板方向稳定、位置重算和视口边界。
- 字号工具条：`80%`–`150%` 范围、`10%` 步进、`A−`/`A+`、恢复 `100%`、控件端点状态、设置持久化，以及 `130%` 以上单列布局适配。
- 登录态模拟：分组列表、左键包含/右键排除及同键取消、仅拦截真实鼠标右键、键盘排除、蓝红状态、焦点保持、重筛中快速取消、两页成员读取、成员数为 0 时仍读取首个分页，以及接口 shape 异常、重复满页、无新增短页后的缓存保留与 UI/localStorage 条件回滚。
- 账号隔离竞态：启动时切号、旧账号数据立即隔离、导航请求未决，以及新账号分组刷新期间再次切号并自动追踪到最新账号。
- 虚拟列表与异步水合：未知类型重判、无 ID 会话隐藏、同/异作者节点复用、同作者不同链接复用、安全迁移和撤销后清理持久记录。
- 键盘与视觉可用性：非模态抽屉的打开/关闭焦点返回、分组重建焦点保持、稍后看删除后的相邻焦点、撤销提示持焦暂停、渐变 SVG 入口结构与稳定尺寸，以及浅色小字和主操作色的 AA 对比度基线。
- 构建产物：ES Modules 被打包为一个 IIFE `.user.js`，最终文件再次通过 JavaScript 语法检查。

## 此前执行的线上只读核对

以下结果来自 2026-09-05 在 `https://t.bilibili.com/` 未登录访客页面进行的只读核对，本次 v3.0.0 未重新在线复核：

- `.bili-dyn-list`：1 个；
- `.bili-dyn-list__items`：1 个；
- `.bili-dyn-list__item` / `.bili-dyn-item`：均有实际动态卡片；
- 卡片头部仍有 `.bili-dyn-item__header`、`.bili-dyn-title__text`、`.bili-dyn-time`，示例卡片还能从 `[data-mid]` 取得作者 UID；
- 图文卡仍有 `.dyn-card-opus`，当前站点资源仍包含视频、直播和 PGC 卡片类名；
- 本次访客示例的 3 张卡片没有公开的外层动态 ID/动态详情链接；脚本会因此只把会话级隐藏绑定到用户实际操作的当前卡片节点，而不会拿正文链接冒充当前动态或按内容指纹连带隐藏其他卡片；
- `GET https://api.bilibili.com/x/relation/tags` 在未登录时返回 HTTP 200、业务码 `-101` 和“账号未登录”，验证了必须检查 JSON 业务码而不能只检查 HTTP 状态。脚本已覆盖此路径。

## 与参考脚本相比重点验证的回归项

- 修正参考脚本缺少协议的 `@match t.bilibili.com/*`。
- 不等待可能不存在的直播/活跃 UP 组件；面板直接挂载到 `document.body`。
- 不使用 0 ms 轮询、100 ms 递归加载或强制滚动。
- 不对每次 DOM 变化全量重扫；新增卡片用 MutationObserver + `requestAnimationFrame` 合批处理。
- 隐藏外层列表项并仅使用自有 `data-btf-*` 属性；关闭脚本或 SPA 离开时完整恢复。
- API 按业务码区分未登录、风控、HTTP 412、超时和网络失败；失败时不误隐藏卡片。
- 只在启用某个分组后按需读取该组成员，最多两个组并发，不启动即抓取全部关注。
- 账号切换先原子隔离旧账号的分组、隐藏与稍后看，再同步新账号；连续切号也会追踪到最新账号。过期成员缓存刷新失败时保留旧缓存，失效分组不会阻塞其他筛选。

## 建议的登录态手工验收

1. 在 Tampermonkey 或 Violentmonkey 安装 `dist/BilibiliTimeline.user.js`，登录后刷新动态首页。Chrome/Edge + Tampermonkey 5.3+ 还需按 [官方 FAQ Q209](https://www.tampermonkey.net/faq.php?q=Q209) 启用“允许用户脚本”或开发者模式。
2. 验证默认分组、特别关注、自定义分组各至少一个；再验证空分组。
3. 验证“两个包含组”“仅排除”“同一作者同时属于包含与排除”三种组合。
4. 向下滚动至少两屏，确认新增卡片自动带有本地操作且继续服从筛选；检查按钮紧跟 UP 主昵称、不覆盖正文，长昵称及触控/窄屏下仍可用。
5. 切换 B 站原生“全部/视频/投稿视频/番剧”等页签，确认面板不重复、过滤继续生效。
6. 在“入口位置”依次选择左下角、右下角、左上角和右上角，刷新后确认选择恢复；缩小窗口，确认入口仍处于同一角且不会落到视口外，顶部两个位置不遮挡 B 站导航栏。
7. 依次选择自动、上、下、左、右展开方向，并在入口靠近各边缘时打开面板，确认首选方向生效且空间不足时能自动避让。
8. 分别收起“关注分组”“隐藏内容类型”“推广与抽奖”“界面与布局”，再同时收起多个模块；确认只留标题、面板平滑重排、筛选仍生效，刷新后折叠状态恢复。
9. 使用 `A−`、滑块和 `A+` 覆盖 `80%`–`150%`，测试恢复 `100%`；在 `130%` 及以上确认设置区变为单列，并刷新确认字号保存。
10. 用明确商品卡、平台搜索口令软广和“关注+转发参与、指定时间开奖”的互动抽奖分别验证高可信隐藏；用普通商品测评、价格比较、反诈和抽奖机制讨论验证不会误伤，并检查疑似内容可以展开。
11. 单击顶部“隐藏”统计卡，确认只显示当前已加载的手动、分组、类型、推广和抽奖隐藏动态并显示原因；再次单击恢复，关闭筛选或离开动态页后也应自动退出该模式。
12. 测试类型、隐藏/撤销、稍后看、主题、密度、加宽和专注模式，并刷新页面确认持久化。
13. 退出登录或断网后刷新，确认状态提示明确、页面内容保持可见、无重复请求。

如果某一步失败，可在浏览器控制台按 `[动态净览]` 过滤日志，并记录失败时的 B 站页签、浏览器、脚本管理器版本和接口业务码；不需要提供 Cookie。
