# 变更日志

本文档记录项目的用户可见变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [3.0.1] - 2026-09-06

### 发布与更新

- 增加明确的 `@updateURL` 与 `@downloadURL`，用户脚本管理器可通过稳定的 GitHub Raw 地址检查并下载新版本。
- 正式版本现在同时发布 Git annotated tag 与 GitHub Release；Release 附带构建完成的 `BilibiliTimeline.user.js` 和 `SHA256SUMS.txt`。
- 项目发布规则已同步记录该流程，后续版本继续保留可安装成品、校验值与版本说明。

## [3.0.0] - 2026-09-06

### 重大变更

- 移除“关键词屏蔽”模块以及 `keywords`、`caseSensitive` 设置字段。导入或读取旧版设置时会安全忽略这些字段。
- 原“关键词屏蔽”的模块折叠状态会迁移给新的“推广与抽奖”模块，其余分组、类型、布局、本地隐藏和本地稍后看数据保持兼容。

### 新增

- 新增可独立折叠的“推广与抽奖”模块，分别控制商品/软广和互动抽奖识别。
- 明确商品组件、购物平台搜索口令、优惠与价格等独立信号会组合判定推广；支持“桃宝/某宝”和 `Copy这条信息` 等常见规避写法。
- 明确抽取行为、关注/转发等参与条件以及开奖或奖品信息会组合判定互动抽奖。
- 高可信内容自动隐藏，中等可信内容默认折叠并允许临时展开；用户也可选择让疑似内容保持正常显示。
- 面板顶部“隐藏”统计卡现在可单击进入“只看隐藏”模式，再次单击恢复普通视图；该模式覆盖手动、分组、类型、推广和抽奖等全部隐藏原因。
- “只看隐藏”会在卡片上显示具体隐藏原因；此模式下单条“隐藏”按钮会暂时禁用，避免把规则隐藏意外改成长期手动隐藏。

### 可靠性与隐私

- 单独出现价格、平台名或“抽奖”等通用词不会触发过滤，并加入普通测评、平台讨论、反诈提醒和抽奖机制讨论等负例测试。
- 商品结构信号只从可信商品组件提取；抽奖规则兼容“抽奖：关注+转发”等常见写法及中英文标点、换行。
- 卡片被虚拟列表复用、折叠控件被页面重绘移除或隐藏卡片离开 DOM 时，会重建必要控件并同步统计，避免出现无法展开或空白的“只看隐藏”视图。
- 推广与抽奖判断完全在浏览器本地执行，不新增网络请求、远程 AI、图片 OCR、`@require` 或跨域权限。
- “只看隐藏”是非持久化查看状态，刷新或离开动态页后恢复普通视图；它只能展示当前页面仍已加载的动态。

## [2.1.1] - 2026-09-06

### 优化

- 将每张动态的“隐藏”和“本地稍后看”操作移到 UP 主昵称右侧，避免工具栏落在时间下方并挡住正文。
- 长昵称会在空间不足时省略，工具栏保持同行；窄屏和触控环境继续保持按钮可见。
- 昵称被链接包裹、更多菜单嵌套或标题行延迟加载时，工具栏会安全放在链接外侧、自动迁入标题行，且不重复创建。

## [2.1.0] - 2026-09-06

### 新增

- “关注分组”、“隐藏内容类型”、“关键词屏蔽”和“界面与布局”现在可分别折叠；折叠后只保留模块标题和展开指示。
- 每个模块的折叠状态都会保存，刷新页面或站内路由切换后仍能恢复。

### 优化

- 采用基于内容高度的平滑折叠过渡，支持系统“减少动效”偏好；一个或多个模块折叠时会实时重算面板位置，避免越界或展开方向跳变。
- 模块标题使用可键盘操作的按钮，同步公布展开状态；如收起正在操作的模块，焦点会返回该模块标题。

## [2.0.0] - 2026-09-06

### 变更

- 浮动入口不再支持自由拖动和方向键像素微调，改为左下、右下、左上、右上四个稳定位置。
- “界面与布局”新增“入口位置”选择，切换后立即生效并持久化；刷新页面或改变窗口尺寸后继续保持在所选角落。
- 左上和右上位置为 B 站固定导航栏预留顶部安全距离；面板原有的自动、上、下、左、右展开方向及越界避让继续保留。

### 兼容

- 首次读取 `1.2.0` 设置时，会按横纵坐标将旧的自由位置自动吸附到最近角落；旧版仅保存左右停靠设置时会迁移到对应的底部角落。
- 设置结构版本升级到 `2`，导入旧版设置仍会执行相同迁移。

## [1.2.0] - 2026-09-06

### 新增

- 彩色渐变入口现在支持基于 Pointer Events 的自由拖动，可使用鼠标、触控笔或触屏调整位置；键盘聚焦入口后也可用方向键微调。
- 入口位置会在拖动结束后持久化，刷新页面时恢复；窗口尺寸变化后会自动夹取到可见视口边界内。
- 面板展开方向新增自动、上、下、左、右五种选择。手动方向作为优先方向，空间不足时会自动避让并保持在视口内。
- 新增界面字号工具条：可用 `A−`、滑块和 `A+` 在 `80%`–`150%` 间按 `10%` 步进调整，也可一键恢复 `100%`。
- 字号设置会自动持久化；达到 `130%` 及以上时，设置区域切换为更适合大字号的单列布局。

## [1.1.1] - 2026-09-05

### 优化

- 将悬浮入口中的文字“滤”替换为用户选定的“漏斗 + 闪光”图标。
- 图标使用青、蓝、紫、粉、珊瑚及橙色渐变，并以内联 SVG 绘制，在高分屏和页面缩放下保持清晰。
- 图标不请求或嵌入外部图片，继续沿用入口按钮的文本无障碍名称。

## [1.1.0] - 2026-09-05

### 优化

- 将分组的“包含/排除”双按钮简化为整行快捷操作：左键包含、右键排除，再次使用相同按键恢复中立。
- 包含状态整行使用蓝色，排除状态整行使用红色，并在行尾显示当前状态。
- 保留分组异步加载期间的取消与切换能力，右键操作后维持键盘焦点。
- 增加 `Enter`/空格包含、`Shift+Enter`/`Shift+空格`排除的键盘操作。

## [1.0.0] - 2026-09-05

首个独立原创实现版本。

### 新增

- 关注分组三态筛选，多包含组取并集，且排除规则优先。
- 关键词普通文本屏蔽和可选大小写敏感匹配。
- 视频、图文、转发、直播、番剧影视和其他类型过滤。
- 卡片级本地隐藏、撤销提示、二次确认的批量恢复与本地稍后看。
- 隐藏记录 30 天过期和 2,000 条上限，本地稍后看 500 条上限及去重。
- 包含筛选统计、状态提示、分组搜索和稍后看抽屉的响应式浮动面板。
- 跟随系统/浅色/深色主题、卡片密度、信息流宽度和隐藏侧栏的专注模式。
- 设置 JSON 导入、导出和二次确认的恢复默认。
- 登录状态检测、按账号隔离的分组与本地数据、分组成员懒加载和默认 6 小时缓存。
- 对 B 站单页应用路由、无限滚动新卡片和多种卡片 DOM 信号的适配。
- 登录失败、接口异常、未知类型和未知作者的容错放行策略。
- 账号切换原子隔离、缓存过期刷新/失败回退，以及异步请求与 SPA 路由切换的竞态保护。
- 稍后看链接规范化及多可信链接去重，可跨追踪参数和 BV/Opus 水合变化保持一致。
- 虚拟列表节点复用连续性校验，避免把上一条动态的会话隐藏迁移到新动态。
- 键盘焦点保持、抽屉删除焦点接续，以及符合 AA 对比度基线的明暗配色。
- 模块化源码、单文件用户脚本构建、语法检查、单元测试与 DOM 测试。

### 隐私与安全

- 脚本运行时不包含遥测、广告、站外请求或远程运行时依赖。
- 只读访问 B 站的登录状态与关注分组接口，不修改账号关系数据。
- 所有用户设置和阅读辅助数据仅保存在 B 站动态域的 `localStorage`。
- 导入数据会经过结构归一化，稍后看链接只允许安全的 B 站 HTTPS 地址。

### 已知限制

- 真实登录账号的端到端验证需在用户自有浏览器会话中手工执行。
- B 站内部接口或 DOM 结构变更可能需要后续适配。
